export interface LythicSettings {
  vaultIntelligenceEnabled: boolean;
  graphEnhancementEnabled: boolean;
}

export const DEFAULT_LYTHIC_SETTINGS: LythicSettings = {
  vaultIntelligenceEnabled: true,
  graphEnhancementEnabled: true,
};
